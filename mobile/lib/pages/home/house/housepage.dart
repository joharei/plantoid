import 'package:flutter/material.dart';

class HousePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Imagine administrating your households :)'),
    );
  }
}
